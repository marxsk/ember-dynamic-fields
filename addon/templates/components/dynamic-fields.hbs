{{#each _source as |element index|}}
  {{yield element index (action 'update')}}
{{/each}}

{{#if _source}}
  {{#if (or (gt max _source.length) (eq max -1))}}
    {{yield (array) _source.length (action 'update')}}
  {{/if}}
{{else}}
  {{yield (array) 0 (action 'update')}}
{{/if}}
